# netlify.toml (raíz del repo)
[build]
  base    = "tech-farming-frontend/dist/tech-farming-frontend"
  publish = "browser"
  command = "npm ci && npm run build -- --configuration=production"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
