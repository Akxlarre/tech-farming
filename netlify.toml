[build]
  base    = "tech-farming-frontend"
  publish = "dist/tech-farming-frontend"
  command = "npm ci --force && npm run build -- --configuration=production"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200