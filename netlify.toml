# netlify.toml (raíz del repo)
[build]
  base    = "tech-farming-frontend"
  command = "npm ci && npm run build"
  publish = "dist/tech-farming-frontend/browser"
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
