# netlify.toml
[build]
  base    = "tech-farming-frontend"     # where your package.json lives
  command = "npm ci && npm run build"   # or your exact build script
  publish = "dist/tech-farming-frontend/browser"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
